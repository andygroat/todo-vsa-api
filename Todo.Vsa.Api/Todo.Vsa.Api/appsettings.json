{
  "Serilog": {
    "Using": [ "Serilog.Sinks.Console" ],
    "MinimumLevel": "Debug",
    "WriteTo": [
      { "Name": "Console" }
    ],
    "Enrich": [ "FromLogContext", "WithMachineName", "WithThreadId" ],
    "Destructure": [
      {
        "Name": "With",
        "Args": { "policy": "Sample.CustomPolicy, Sample" }
      },
      {
        "Name": "ToMaximumDepth",
        "Args": { "maximumDestructuringDepth": 4 }
      },
      {
        "Name": "ToMaximumStringLength",
        "Args": { "maximumStringLength": 100 }
      },
      {
        "Name": "ToMaximumCollectionCount",
        "Args": { "maximumCollectionCount": 10 }
      }
    ],
    "Properties": {
      "Application": "Sample"
    }
  },

  "AllowedHosts": "*",

  "ConnectionStrings": {
    // Example connection string for Azure SQL Database with Active Directory Interactive authentication
    // "TodoContext": "Server={xyz}.database.windows.net; Initial Catalog={db name}; Authentication=Active Directory Interactive; MultipleActiveResultSets=true;Connection Timeout=30;Connection Lifetime=0;"
    // Example connection string for Azure SQL Database with Active Directory Password authentication
    // "TodoContext": "Server={xyz}.database.windows.net; Initial Catalog={db name}; Authentication=Active Directory Password; User ID={user id}; Password={password}; MultipleActiveResultSets=true;Connection Timeout=30;Connection Lifetime=0;"
    // Example connection string for Azure SQL Database with Managed Identity authentication
    // "TodoContext": "Server={xyz}.database.windows.net; Initial Catalog={db name}; Authentication=Active Directory Managed Identity; MultipleActiveResultSets=true;Connection Timeout=30;Connection Lifetime=0;"
    // Example connection string for SQL Server with SQL Server Authentication
    // "TodoContext": "Server={server name}; Initial Catalog={db name}; User ID={user id}; Password={password}; MultipleActiveResultSets=true;Connection Timeout=30;Connection Lifetime=0;"
  }
}
